arr = input("What is the arr? ")
# length = arr.split(", ")


# pivot = arr[len(length) // 2]

# left = [x for x in arr if x < pivot]
# middle = [x for x in arr if x == pivot]
# right = [x for x in arr if x > pivot]
# result = left + middle + right
# print(f"{result}")